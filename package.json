{
  "name": "claude-system",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "hooks-server": "bun run hooks-server/server.ts",
    "verify": "bun run agents/verifier.ts",
    "quality-scorer": "bun run agents/quality-scorer.ts",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0",
    "@types/pg": "^8.11.10",
    "pg": "^8.13.3"
  }
}
